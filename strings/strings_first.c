/*
So far, we have been declaring variables that hold numbers or
characters.
What if we wanted to store text in a variable?
As we already know, text should be enclosed in double quotes and
is called a string.
*/

#include <stdio.h>

int main(){
    printf("This is some text.");
    
    return 0;
}