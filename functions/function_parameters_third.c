/*
Multiple Parameters
Functions can take multiple parameters. For that, we simply
need to separate them using commas, for example:

*/

void greeting(char name[], int age){
    printf("Hello, %s!\n", name);
    printf("You are %d years old. \n\n", age);
}